export class ReelData {
    constructor(id, ownerId, textColor, backgroundColor){
        this.id = id
        this.ownerId = ownerId
        this.textColor = textColor
        this.backgroundColor = backgroundColor
    }
}
